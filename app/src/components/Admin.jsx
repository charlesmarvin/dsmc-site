var React = require('react');

var Admin = React.createClass({
    render() {
        return (
            <div className="units-row">
            </div>
        );
    }

});

module.exports = Admin;
